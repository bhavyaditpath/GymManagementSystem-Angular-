import { NoWhitespaceEmailDirective } from './no-whitespace-email.directive';

describe('NoWhitespaceEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new NoWhitespaceEmailDirective();
    expect(directive).toBeTruthy();
  });
});
